pyodbc>=5.0
pandas>=2.0
matplotlib>=3.7
PySide6>=6.5

# LLM_BACKEND=local (packaged build): loads models/*.gguf in-process, no
# Ollama install or server required. See README.md "Local model" section.
llama-cpp-python>=0.3.0
