__pycache__/
*.py[cod]
.vscode/

# Phase 5 verification venv (Python 3.12, for llama-cpp-python's prebuilt-wheel
# requirement) -- local tooling, not source.
.venv*/

# Test output: regenerated by the phase test scripts on every run.
charts/
screenshots/

# The Ollama installer, downloaded during setup. 1.4 GB; not source.
OllamaSetup.exe

# Bundled local-inference model weights, ~4-5 GB; not source. See README.md
# "Local model" section for how to fetch it.
models/*.gguf

# Phase 6 PyInstaller output: the shareable build (~4.6GB with the model),
# rebuilt from db_whisperer.spec, not source.
build/
dist/
